import { createApp } from "./basics";

// 采用完全默认配置
createApp();

// 页面数据加载时显示默认加载页
// createApp({ renderLoading: true });

// 页面数据加载时使用自定义的加载页
// createApp({
//   renderLoading() {
//     return "页面数据加载中";
//   },
// });
